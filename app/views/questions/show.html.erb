<h1><%= @question.title %></h1>
<p><%= @question.body %></p>
<p>View Count: <%= @question.view_count %></p>


<%= link_to "Edit", edit_question_path(@question) %> &nbsp <%= link_to "Delete", question_path(@question), data: {confirm: "Are you sure?"}, method: "delete" %>
